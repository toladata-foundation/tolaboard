import { matchesGraphInput } from 'tolaboard/helpers/matches-graph-input';
import { module, test } from 'qunit';

module('Unit | Helper | matches graph input');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = matchesGraphInput([42]);
  assert.ok(result);
});
