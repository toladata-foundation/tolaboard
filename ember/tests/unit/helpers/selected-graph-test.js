import { selectedGraph } from 'tolaboard/helpers/selected-graph';
import { module, test } from 'qunit';

module('Unit | Helper | selected graph');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = selectedGraph([42]);
  assert.ok(result);
});
